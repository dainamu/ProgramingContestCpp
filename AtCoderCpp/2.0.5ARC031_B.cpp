#include <iostream>
#include <vector>
#include <algorithm>

#define req(i, n) for(int i=0; i<n; i++)

using namespace std;

int main() {

	vector<string> a(10);

	req(i, 10) cin >> a.at(i);

	





	return 0;
}