#include <iostream>
#include <vector>
#include <algorithm>

#define req(i, n) for(int i=0; i<n; i++)

using namespace std;
using ll = long long;

int main_sample() {

	req(i, 10) {
		cout << i << endl;
	}
	return 0;
}