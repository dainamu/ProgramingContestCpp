#include <iostream>
#include <vector>
#include <algorithm>

#define req(i, n) for(int i=0; i<n; i++)

using namespace std;
using ll = long long;

int main_sample() {

    int a;
    cin >> a;

    if (a % 3) { // 0‚Ífalse
        cout << "Hello World!" << endl;
    }
    else {
        cout << "foo" << endl;
    }
    return 0;
}